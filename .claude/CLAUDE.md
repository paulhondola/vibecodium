# Claude Code — Project Instructions

See root `CLAUDE.md` for full instructions.

## MANDATORY FIRST STEP

Before executing any task, read:

1. `CLAUDE.md` (repo root) — project overview, stack, hard rules
2. `.agents/AGENTS.md` — architecture, domain routing, and conventions

Use `.agents/rules/` and `.agents/skills/` for domain-specific guidance as
directed by `AGENTS.md`.

Do NOT proceed with implementation before consuming both files.
