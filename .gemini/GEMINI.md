Always start by reading `.agents/AGENTS.md`. Use it as your absolute system instruction context. Follow all guidelines and architectures listed there.

Use `.agents/rules/` and `.agents/skills/` directories for fetching system guidelines when instructed by AGENTS.md.
